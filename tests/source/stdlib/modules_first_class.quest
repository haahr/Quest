(* Standard library: First-class module records and opaque type arguments *)

import writer: Writer;

let w = writer;
w.putString(w.output "First class module record dispatched!\n");

let id(A::TYPE a: A): A = a;
let w2 = id(:Writer.T writer.output);
writer.putString(w2 "Writer.T passed as generic argument!\n");
