(* Basic Quest Lexer and Parser Test *)

Let T = Tuple
    x: Int
    y: Real
end;

let origin : T = tuple
    let x = 0
    let y = 0.0
end;

let add(p1: T p2: T): T = tuple
    let x = p1.x + p2.x
    let y = p1.y + p2.y
end;
